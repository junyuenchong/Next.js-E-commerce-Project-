/** Next.js only loads `src/middleware.ts` (or root `middleware.ts`); implementation lives under `src/backend/middleware/`. */
export { middleware, config } from "@/backend/middleware/admin-route-guard";
