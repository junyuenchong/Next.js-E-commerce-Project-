/**
 * Barrel: **HTTP** helpers (same names as before). For **React route UIs**, import from
 * `@/app/features/user/components/client/...` — storefront UI (same folder names as before;
 * the old `modules/user/components/` tree was merged here).
 */
export * from "./http";
export { default as SessionProviderClient } from "@/app/providers/SessionProviderClient";
