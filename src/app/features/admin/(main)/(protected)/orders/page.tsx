/** Admin: order list. */
import AdminOrdersClient from "@/app/features/admin/components/client/orders/AdminOrdersClient";

export const metadata = { title: "Orders · Admin" };

export default function AdminOrdersPage() {
  return <AdminOrdersClient />;
}
