"use client";

import React from "react";

export default function UserLayoutClient({ children }: { children: React.ReactNode }) {
  // You can provide user context or any client-side logic here if needed
  return <>{children}</>;
} 